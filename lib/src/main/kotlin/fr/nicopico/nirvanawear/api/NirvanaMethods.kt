package fr.nicopico.nirvanawear.api

internal enum class NirvanaMethods(
    val value: String
) {
    Everything("everything"),
    Prefs("prefs"),
    Tasks("tasks"),
    Tag("tags"),
    User("user"),
}
